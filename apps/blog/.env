# 主题配置
NUXT_THEME=kmatrix

# 接口基准地址。线上部署建议留空（使用相对路径），或在 EdgeOne 控制台中配置环境变量。
# 填入 / 或留空均表示使用相对路径，避免写死 localhost 导致线上报错。
NUXT_PUBLIC_API_BASE_URL=/

# 后端接口直连地址（服务端代理用）
# 线上部署请在 EdgeOne 控制台中配置 NUXT_BACKEND_URL 环境变量。
BLOG_API_URL=http://localhost:8080
